// Centralised canvas configuration — board bounds, persisted keys, and visual tokens.

export const WORLD_W = 4000;
export const WORLD_H = 3000;

// Remembered drop choices (localStorage).
export const URL_CHOICE_KEY = "meko.urlDropChoice"; // image vs link for a pasted page URL
export const EMBED_CHOICE_KEY = "meko.embedDropChoice"; // link-with-preview vs embed for a provider URL

// Dot-grid colour (radial-gradient dot).
export const GRID_DOT_COLOR = "#b4bccb";
